/*
repeat() funciona para repetir certa string x quantidade de
vezes.
se for um número negativo ou infinito dentro do range
dará um erro, um numero decimal será aredondado para o inteiro
mais próximo, e 0 será uma string vazia
*/

let word = "Hello!";
let repeatedWord = word.repeat(3);
console.log(repeatedWord);